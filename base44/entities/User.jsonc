{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "enum": [
        "superadmin",
        "admin",
        "user"
      ],
      "default": "user",
      "description": "Rol global en la plataforma"
    },
    "phone": {
      "type": "string",
      "description": "Tel\u00e9fono de contacto"
    },
    "rbac_role_id": {
      "type": "string",
      "description": "ID del rol RBAC asignado (referencia a entidad Role)"
    }
  },
  "required": [
    "phone"
  ]
}